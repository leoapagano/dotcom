FOR ALL PAGES:
- add top navigation bar
    - "leoapagano.com" text on left side
    - hamburger menu (may become a full top bar on some platforms) on right side that points to:
        - rows (text with icons):
            - home
            - about me
            - blog
            - projects (expands)
        - bottom (as icons):
            - github
            - linkedin
            - light/dark mode switch
- create page content (not the actual page, the text on it):
    - home
    - about me
    - blog home
    - projects home

PAGE SPECIFIC:
- home page design

MISC:
- make leoapagano.dev point to leoapagano.com/projects

